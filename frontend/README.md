# Frontend (Vanilla JS)

## Run
1. Start backend API first (`http://127.0.0.1:8000`).
2. In a new terminal:
   - `cd frontend`
   - `python -m http.server 5173`
3. Open `http://127.0.0.1:5173`.

If your backend uses a different host/port, update `API URL` in the UI.
